# ProfitPilot
Bookkeeping for small local businesses. Flat project layout — all files at root.
Run locally: npm install, then npm run dev.
Deploys on Vercel automatically (Vite).
schema.sql = the database setup script for Supabase (SQL Editor -> paste -> Run).
